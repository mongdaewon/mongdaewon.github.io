# Claude Code 프로젝트 adapter

이 파일은 Claude Code 전용 adapter다. 프로젝트 규칙을 복제하지 않는다.

## 공통 지침

먼저 전역 공통 지침을 읽는다.

```text
~/.agents/AGENTS.md
```

그 다음 이 저장소의 다음 파일을 프로젝트 지침의 정본으로 읽고 따른다.

```text
AGENTS.md
```

## Claude 전용

- Claude 전용 기능은 전역 `~/.claude/` 설정과 리소스를 따른다.
- 프로젝트 `.claude/` 아래 리소스가 있으면 Claude 호환 레이어로만 사용한다.
- 공통 지침과 프로젝트 규칙을 이 파일에 다시 쓰지 않는다.
